[project]
name = "joscam"
version = "0.1.0"
description = "Real-time virtual camera enhancements for Linux"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "opencv-python",
    "pyside6>=6.11.1",
    "pyvirtualcam",
]
